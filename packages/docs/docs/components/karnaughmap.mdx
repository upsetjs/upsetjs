---
title: Karnaugh Map
---

UpSet.js also has a variant of a Karnaugh Map for rendering set intersections in a dense matrix way.

```jsx live
// import React from 'react';

function Default() {
  return <KarnaughMap sets={GameOfThronesDataSet.sets} width={780} height={500} />;
}
```

## Skeleton

```jsx live
// import React from 'react';

function Skeleton() {
  return <KarnaughMapSkeleton width={780} height={500} />;
}
```

## Title

```jsx live
// import React from 'react';

function Title() {
  return (
    <KarnaughMap
      sets={GameOfThronesDataSet.sets}
      width={780}
      height={500}
      title="Game of Thrones"
      description="Information about the characters of the tv series Game of Thrones"
    />
  );
}
```

## Interactivity

```jsx live
// import React from 'react';

function Interactivity() {
  const [selection, setSelection] = React.useState(null);
  return (
    <KarnaughMap
      sets={GameOfThronesDataSet.sets}
      width={780}
      height={500}
      selection={selection}
      onHover={setSelection}
    />
  );
}
```

## Click

```jsx live
// import React from 'react';

function Click() {
  const [selection, setSelection] = React.useState(null);
  return (
    <KarnaughMap
      sets={GameOfThronesDataSet.sets}
      width={780}
      height={500}
      selection={selection}
      onClick={setSelection}
    />
  );
}
```

## Queries

```jsx live
// import React from 'react';

function Queries() {
  return (
    <KarnaughMap
      sets={GameOfThronesDataSet.sets}
      width={780}
      height={500}
      queries={GameOfThronesDataSet.queries}
      queryLegend
    />
  );
}
```

## Small Sets

```jsx live
// import React from 'react';

function SmallSets() {
  return (
    <div>
      <KarnaughMap
        width={780}
        height={500}
        sets={asSets([
          { name: 'one', elems: [1, 2, 3, 5, 7, 8, 11, 12, 13] },
          { name: 'two', elems: [1, 2, 4, 5, 10] },
          { name: 'three', elems: [1, 5, 6, 7, 8, 9, 10, 12, 13] },
        ])}
      />
      <KarnaughMap
        width={780}
        height={500}
        sets={asSets([
          { name: 'one', elems: [1, 2, 3] },
          { name: 'two', elems: [1, 2, 4, 5] },
          { name: 'three', elems: [1, 5] },
        ])}
      />
    </div>
  );
}
```
